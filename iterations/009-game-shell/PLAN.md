# 009-game-shell: рабочий план

Начата 2026-09-24 из завершённой 008-race-loop. Статус: в работе, тега нет.
Снимки 001–008 и local/game неизменны. Runs начинаются с 001.

## Цель и критерии

Общая игровая оболочка, профиль/сохранения и первые сквозные события Evolution
и Factory Driver. Полная постановка: [docs/next-iteration.md](../../docs/next-iteration.md).
Критерии: меню → профиль → событие → результат → запись → перезапуск → продолжение;
обе карьеры, native/web, регресс управления, сборки и визуальная приёмка.

## Задачи и владельцы

- [x] T00: инверсия 6 DOF и регресс управления — интеграция завершена.
  Исполнитель physics закончил vehicle.rs: отрицательный угол для правого поворота
  при локальном направлении -Z; исправлены физический и визуальный повороты колёс.
  Тест направления падал до исправления, проходит после. Web input tests проходят.
  118 Rust-тестов passed, 3 GPU ignored; fmt/clippy чистые, native/WASM release собраны.
  Chromium: реальный ввод → WASM → направление в обеих моделях, UI/focus — passed;
  снимки осмотрены. [Run 001](runs/001-controls-regression/README.md).
  Исполнитель завершил работу, активных назначений нет.
- [ ] T01: модель экранов и общая навигация — не начато.
- [ ] T02: профиль, надёжное сохранение и восстановление — не начато.
- [ ] T03: подтверждение правил событий и связей ресурсов — не начато.
- [ ] T04: первые сквозные события обеих карьер — не начато.
- [ ] T05: базовый звук, интеграция и приёмка — не начато.

## Факты и ограничения

Ошибка пользователя относится к 008, а не 005. Диагностика в 005 была ошибочной;
собственные изменения восстановлены из Git, посторонние артефакты убраны в local.
В 6 DOF знак поворота колёс расходился с контрактом положительного правого руля.
Аркадный yaw имел правильный знак. Это исправление реализации, не реверс оригинала.
Содержимое карьерных заданий из плана требует проверки; гипотезы не выдавать за факты.

## Проверки

```powershell
. ./scripts/tool-env.ps1
cargo fmt --manifest-path iterations/009-game-shell/Cargo.toml --all -- --check
cargo test --locked --manifest-path iterations/009-game-shell/Cargo.toml --target-dir local/builds/009-game-shell/.cargo-target --workspace
cargo clippy --locked --manifest-path iterations/009-game-shell/Cargo.toml --target-dir local/builds/009-game-shell/.cargo-target --workspace --all-targets -- -D warnings
node iterations/009-game-shell/web/ui.test.mjs
./scripts/build.ps1 -Iteration 009-game-shell -Config release -Target all
```

Ближайший шаг: T01 — модель экранов; T03 — проверка правил первых событий карьер.
Отдельная интерактивная проверка native-окна в T00 не проводилась; ввод покрыт
unit tests, общий код физики проверен численно и через WASM.
