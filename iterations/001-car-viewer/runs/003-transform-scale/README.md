# Run 003: single transform scale

- Дата: 2026-09-17
- Автомобиль: `356b`
- Цель: проверить двойное масштабирование координат и `tr`.

## Изменение

`mesh()` уже делил вершины на `5` и добавлял `tr`, после чего loader ошибочно
делил результат ещё раз. Второе деление удалено; перенос `tr` также проходит
через преобразование оси Z.

## Результат

Машина всё ещё развалена: одного исправления масштаба недостаточно. Варианты
панелей `In`/`Out` и их назначение требуют отдельного сопоставления.
