# План восстановления Porsche Unleashed

## Цель и постоянная документация

Полный объём переноса, включая Evolution, Factory Driver, остальные режимы,
сохранения, звук и все целевые ОС: [docs/roadmap.md](docs/roadmap.md).
Это целевой результат проекта; просмотрщик и ArcadeCar — промежуточные инструменты.

Восстановить поведение оригинальной PC-игры и создать самостоятельный движок
на Rust + wgpu + winit для Windows, Linux, macOS и браузера (WebAssembly/WebGPU). Первая функциональная веха —
машина `356a` с оригинальными текстурами, вращением и приближением камеры.

Поддерживать README.md (навигация и запуск), PLAN.md (актуальный план),
STATUS.md (состояние и следующий шаг), docs/ (окружение, форматы, решения).
Документация на русском. При начале итерации записывать цель; после открытия,
изменения решения и проверок обновлять документы; перед завершением сеанса
актуализировать статус. Факты отделять от гипотез. Для бинарников сохранять
SHA-256, модуль и RVA; для внешнего кода — источник, коммит и условия использования.

## Git и итерации

Один локальный Git-репозиторий, main, без удалённого репозитория. Каждая большая
итерация — полный независимый снимок исходников, Cargo workspace, тестов,
конфигураций, lock-файлов и документации в iterations/NNN-name.
Следующая создаётся копированием предыдущей без сборок и кэшей. Межитерационные
зависимости запрещены. Завершённые снимки неизменны. Исправления — в следующей.
README снимка содержит цель, статус, состав, отличия, требования, команды,
результаты, ограничения и тег. При закрытии — снимок плана и документации,
коммит и аннотированный тег iteration-NNN. Не закрывать этап с невыполненными
критериями. Скрипт новой итерации запрещает перезапись существующей папки.

Игровые данные, инструменты, сохранения, базы Ghidra и кэши — вне Git в local/.
Роли агентов — в проекте. Исходный каталог игры только читается.
Все итерации и новый движок читают одну общую папку local/game. Нельзя копировать
ресурсы в снимки или браузерный пакет. Дополнительная копия допускается только
для эксперимента, который изменяет файлы (включая запуск оригинала).
Сильная доступная модель координирует; исполнители выбираются по сложности
из любых провайдеров. Принципы назначения: docs/agents.md, без привязки к брендам.

## Подготовка окружения (вне итераций)

По решению пользователя 2026-09-18 пустой каркас `000-bootstrap` убран из
`iterations`; восстановимая копия находится в `local/archive/000-bootstrap`.
Первый функциональный снимок — `001-car-viewer`. Результаты подготовки
сохраняются в `docs`, отдельный прототип или тег `iteration-000` не нужен.

1. Сохранить существующие Git и Python; установить Rust/rustup (MSVC), rustfmt,
   Clippy; MSVC Build Tools x86/x64 и Windows SDK; CMake, Ninja; официальный
   Ghidra и совместимый JDK; x64dbg/x32dbg. Закрепить Rust и записать версии.
2. Переносимые инструменты хранить в local/tools; системные ставить штатно.
   Проверить сборку минимальной Rust-программы и headless-запуск Ghidra.
3. Скопировать игру из `C:\Program Files (x86)\by Decepticon\Need for Speed - Porsche Unleashed`
   в local/game; сверить каждый файл по размеру и SHA-256. Сохранить манифест.
   Для запусков и изменений создавать копии в local/experiments.
4. Описать модифицированную сборку (version.txt: 3.5; nfs5.exe:
   2019.06.21.027; SilentPatch и ASI Loader). Импортировать основные EXE/DLL
   в Ghidra и установить реально используемые при запуске модули.

Готовность: инструменты проверены, Git настроен, копия сверена, сборка описана,
каркас собирается, документация позволяет повторить подготовку.

## 001-car-viewer

Текущий рабочий план и передача между моделями:
[`iterations/001-car-viewer/PLAN.md`](iterations/001-car-viewer/PLAN.md).

2026-09-18: run010 — 44/44 модели, 88 снимков осмотрены; style1 исправил колёса
и f901, mt+0x114 исправил перекрытие фар, маска окраски сохраняет лампы/хром.
30 unit + 3 GPU tests, native/WASM build пройдены. 2026-09-19 пользователь
подтвердил браузер и принял закрытие. Native-окно отдельно интерактивно не проверено;
native подтверждён offscreen Vulkan. Принятый снимок: `iteration-001`.

Использовать OpenNFS/LibOpenNFS, CrpLib и dima424658/LibOpenNFS как закреплённые
справочные реализации; собственные загрузчики на Rust, без C++ в runtime.

Workspace: nfs-formats (распаковка и форматы, без GPU), nfs-assets (части,
геометрия, материалы, изображения), porsche-viewer (CLI, окно, камера, ввод).
Команды: inspect --game-dir <path> --car 356a; view --game-dir <path> --car 356a.

Восстановить распаковку CRP, части, варианты/LOD, вершины, нормали, UV, индексы,
встроенные текстуры и связи материалов. Проверить роль FSH, .tpg и .clr по
данным и оригиналу, не угадывать по имени. Непонятные обязательные поля
исследовать через Ghidra/x32dbg. Ошибка парсера указывает файл и смещение.
Выбрать неповреждённую комплектацию с максимальной доступной детализацией.

Уточнение 2026-09-17: довести эту итерацию до корректной базовой сборки всех
моделей из ресурсов `CarModel`, включая полицейские и низкодетальные модели.
Выбирать статьи по `Base`, доступным уровням и связанным `pr`, проверять весь
локальный набор и сохранять визуальные проверки в `runs`. Дополнительные хоткеи
не входят в этот этап.

Vulkan на Windows/Linux, Metal на macOS. Базовое освещение, текстуры, глубина,
прозрачность; вращение мышью, масштаб колесом, R — сброс; resize/minimize.
Преобразование координат — в слое ресурсов. Точные отражения и физика позже.

Дополнение пользователя: локальный сайт без публикации. Общие загрузчики и
рендерер собираются в WebAssembly; браузер выбирает игровые файлы локально,
без передачи на сервер. Один сценарий сборки создаёт Windows executable и
браузерный пакет. Отдельный локальный HTTP-сервер слушает только localhost,
обслуживает только сайт. WebGPU требует совместимый браузер; показывать понятную
ошибку при отсутствии API. Игровые данные не включать в сайт или git.

Готовность: самостоятельный просмотрщик отображает все 44 базовые модели CarModel
без известных дефектов, с проверенными текстурами и управляемой камерой.
Native/WASM сборки и графические smoke-тесты обязательны. Заглушки этап не закрывают.

Приёмка 001: браузер подтверждён пользователем, native — offscreen GPU;
неподтверждённый интерактивный native smoke сохранён как ограничение снимка.

## 002-track-viewer (завершена и проверена)

Рабочий план и состояние:
[`iterations/002-track-viewer/PLAN.md`](iterations/002-track-viewer/PLAN.md).

Снимок создан из `iteration-001` (скрипт `scripts/new-iteration.py`). Тег снимка: `iteration-002`.
2026-09-22: run001 — базовый участок `skidpad` (дорожное полотно, стык блоков `RD0040C`/`RD0048C`
без швов и разрывов, объекты окружения: шинные отбойники `tir1`, каменная стена, деревья, строения).
run002 — исправление формата 16bpp FSH `0x7e` (ARGB 1555), устранение фиолетовой пикселизации на всех треках,
батчинг мешей по материалам (-97% буферов), визуальная проверка на общих планах и с сильным приближением (zoom-in).
37 unit tests, clippy чист, fmt проверен, native и WASM сборки пройдены.
Парсер `karT` CRP (`track.rs`), триангуляция типов примитивов 1, 3, 4, сопоставление текстур FSH (100% материалов).
15/15 трасс игры успешно загружаются; нулевая регрессия автомобилей (44/44 модели 001 загружаются).
## 003-track-environment (завершена и проверена)

Рабочий план и состояние:
[`iterations/003-track-environment/PLAN.md`](iterations/003-track-environment/PLAN.md).

Снимок создан из `iteration-002` (скрипт `scripts/new-iteration.py`). Тег снимка: `iteration-003`.
2026-09-22: run001 — визуальное окружение `skidpad`: расстановка динамических библиотечных объектов трека (`Base & 0x8000 != 0`) по файлу сценария `.scn` (конусы `CONE`, стартовая стрелка `ARW1`), рендеринг купола неба `horz` из `Sky/Skidpad.fsh` (цилиндрическая 360° развёртка двух тайлов 256x128 с плавным затуханием в надир).
Разделение статической геометрии (`static_mesh_count`) и шаблонов пропов (`PropArticle`), отрисовка экземпляров через проход инстанцирования (`PropDraw`), исключающий парение объектов в центре трека.
40 unit tests, clippy чист, fmt проверен, native и WASM сборки собраны.
Нулевая регрессия: 44/44 автомобиля и 15/15 трасс игры успешно загружаются.

## 004-track-topology (завершена и проверена)

Рабочий план и состояние:
[`iterations/004-track-topology/PLAN.md`](iterations/004-track-topology/PLAN.md).

Снимок создан из `iteration-003` (скрипт `scripts/new-iteration.py`). Тег снимка: `iteration-004`.
Цель: парсинг топологии трассы, графа соединений `.jnc`, границ дорожного полотна `.edg` и секций `.map`, построение линий границ полотна, их интерактивный рендеринг и навигация по трассе на спорткаре.
2026-09-23: run001 — offscreen Vulkan-снимки треков `skidpad` (167 сегментов границ полотна / 121 непрерывная линия) и `autobahn` с наложенными линиями полотна без z-fighting, регрессионный снимок автомобиля `356a` (0 регрессий).
Реверсированы бинарные структуры `.jnc` (36 байт/запись) и `.edg` (28 байт/запись). Скрипты реверса сохранены в `scripts/research/`. Разработаны парсеры `nfs-formats/src/topology.rs` для всех 15 трасс игры.
Интегрирован рендерер линий `TopologyRenderer` с шейдером на WGSL и полигональным смещением глубины.
Веб-интерфейс: автозагрузка ресурсов по дефолту из `local/game` (без необходимости выбирать файлы вручную), сохранение кнопок ручной загрузки для переопределения, нативный выбор трасс и авто через `<select id="targetSelect">` в один клик, чекбокс «3D Границы» и горячая клавиша `T`.
Аркадная навигация по трассе (`ArcadeCar`): спорткар Porsche (`car_mesh.rs`) со сменой цветов палитры (`P`), физика движения/руления/заноса с ручным тормозом, расчёт КПП и оборотов двигателя, автоматическая привязка к высоте и уклону дороги, камера от 3-го лица с переключением видов (`C`), гоночный полупрозрачный HUD со спидометром, передачей и тахометром в браузере.
53 unit tests, clippy чист (`-D warnings`), fmt проверен, release native и WASM сборки собраны. Управление, скорость и текстуры проверены в браузере.
Принятый снимок: `iteration-004`. Следующий этап — `005-track-physics-surfaces` (подробности в docs/next-iteration.md).

## 005-unified-driving (завершена и проверена)

План: [iterations/005-unified-driving/PLAN.md](iterations/005-unified-driving/PLAN.md). Тег: `iteration-005`.
Приоритет уточнён пользователем: единый desktop/web запуск, скрываемый UI,
плавное управление и движение к полному реверсу. Сохранён native CLI,
добавлен desktop app с общим web UI и каталог в native окне.
Шинная модель, следы и таймер из прежнего плана перенесены после проверки
семантики данных и измерений оригинала. ArcadeCar остаётся прототипом.
Следующий исследовательский этап: [docs/next-iteration.md](docs/next-iteration.md) (006).

Результат: spatial grid дорожных треугольников прошёл все 15 трасс (ошибка до
0.000164 м); 72 unit tests, fmt/clippy, native/WASM release и 44 авто проверены.
Playwright Chromium подтвердил web UI/заезд/focus, native offscreen снимки осмотрены.
Интерактивный запуск native-окна и desktop-лаунчера проверен и подтверждён пользователем.
Принятый снимок: `iteration-005`.

## 006-game-systems (готова к приёмке, 2026-09-23)

План: [iterations/006-game-systems/PLAN.md](iterations/006-game-systems/PLAN.md).
Цель: доказательная карта поведения оригинальной игры (NFS 5) и разработка парсеров
её ключевых форматов данных (`Simulation`, `FEData`, `savedata`). Подготовка доказательного
фундамента для симуляции физики (007) и режима заезда/карьеры (008–010).

- Снимок создан из `iteration-005` (скрипт `scripts/new-iteration.py`).
- T01: PE-карта `nfs5.exe`, функции загрузки `.sim` (`0x0049c750`), `.ais` (`0x0049ca30`), масштабирующие коэффициенты. См. [`simulation-evidence.md`](iterations/006-game-systems/research/simulation-evidence.md).
- T02: парсеры `SimCar` (328 байт) и `AisCar` (304 байта) в `nfs-formats/src/sim.rs`. Валидация 88/88 `.sim` и 22/22 `.ais` файлов.
- T03: архитектура сохранений `.sav` (голова связного списка `0x0065b634`), парсеры каталогов `nfs5.car` (109 авто), `nfs5.trk` (15 трасс), `nfs5.fac` (34 миссии Factory Driver) в `nfs-formats/src/career.rs`. См. [`career-evidence.md`](iterations/006-game-systems/research/career-evidence.md).
- T04: реверс детерминированных реплеев `replay.rpl` (8 суб-сэмплов на тик, RLE-поток ввода, заголовок 15 908 байт на 8 авто), парсер `ReplayFile` в `nfs-formats/src/replay.rs`, открытие 500-RPM сетки крутящего момента, baseline стенд в `local/experiments/bench/sim_baseline.json`. См. [`telemetry-evidence.md`](iterations/006-game-systems/research/telemetry-evidence.md).
- T05: интеграция, 83 unit-теста, `fmt` и `clippy` (`-D warnings`) чистые, полная сборка `build.ps1 -Target all` (native + WASM), артефакты Run 001 (`runs/001-game-systems/`).
- Следующий этап: `007-physics-simulation` (подробности в docs/next-iteration.md).


## Проверки и продолжение

Тесты декомпрессора и парсеров: корректные/обрезанные данные, размеры, смещения,
индексы. Сверка распаковки со справочным декодером, геометрии и материалов —
с оригиналом. Проверить 356a с нескольких ракурсов и дополнительно 356b.
CI: Windows/Linux x86-64, macOS ARM64; синтетические ресурсы. Реальные файлы —
локально. Различать сборку и проверенный графический запуск. Доступ к другим
ОС пока не подтверждён. При закрытии сохранять команды и результаты проверок.

Дальнейшие вехи: трасса → измерения оригинала → физика без графики → гонка с AI
→ остальные режимы, звук, меню, сохранения. Каждый этап получает полный снимок
и уточнённые критерии. Побайтовое совпадение EXE не требуется. Численную
совместимость x86/ARM и допуски определять экспериментально.
